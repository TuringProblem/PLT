## Idea

Each month is going to contains a set of folders for the data and has `data.md` file for each day. 

My goal is to make a parser that will take all the data and create an IR for typst to reder inside `./lib/`
