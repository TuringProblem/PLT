= how to use Curl

#let code(text) = {
  block(fill: rgb(220, 220, 220), width: 100%, inset: 8pt, radius: 4pt)[#text]
}


#code([
```bash
# Commands to use after: curl <commands> ->

-I # just gets the header information (not the content)
-i # Gets both header and content




```
])
